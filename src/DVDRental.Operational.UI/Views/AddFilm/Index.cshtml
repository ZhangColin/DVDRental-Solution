@model DVDRental.Operational.UI.Models.FilmModel

@{
    ViewBag.Title = "添加影片";
}

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true, "Film creation was unsuccessful. Please correct the errors and try again.")
    <div>
        <fieldset>
            <legend>影片信息</legend>

            <div class="editor-label">
                @Html.LabelFor(m => m.Title)
            </div>
            <div class="editor-field">
                @Html.TextBoxFor(m => m.Title)
                @Html.ValidationMessageFor(m => m.Title)
            </div>

            <p>
                <input type="submit" value="添加" />
            </p>
        </fieldset>
    </div>
}