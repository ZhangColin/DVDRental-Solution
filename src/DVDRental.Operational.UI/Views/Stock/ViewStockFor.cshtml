@using DVDRental.Fulfillment.Stock
@model DVDRental.Operational.UI.Models.StockModel

@{

}

<table class="table table-striped table-bordered table-condensed">
    <tr>
        <td>Id</td>
        <td>条形码</td>
        <td>状态</td>
    </tr>

    @foreach (Dvd dvd in Model.Stock) {
        <tr>
            <td>@dvd.Id</td>
            <td>@dvd.Barcode</td>
            <td>
                @if (dvd.CurrentLoan != null) {
                    <i>借给 @dvd.CurrentLoan.SubscriptionId</i>
                }
            </td>
        </tr>
    }

</table>

@using (Html.BeginForm("AddStockFor", "Stock", new { FilmID = Model.FilmId })) {
    <p><input class="btn" value="添加库存" type="submit" /></p>
}   